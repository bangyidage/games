<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.npb.dao.UserMapper">
    <select id="getUserList" resultType="com.npb.pojo.User">
        select * from news.user
    </select>
    <select id="getUserById" parameterType="int" resultType="com.npb.pojo.User">
        select * from news.user where id = #{id}
    </select>
    <insert id="addUser" parameterType="com.npb.pojo.User">
        insert into news.user (id, username, password) values (#{id},#{username},#{password});
    </insert>
    <update id="modifyUser" parameterType="com.npb.pojo.User">
        update news.user set username=#{username},password=#{password}  where id=#{id}
    </update>
    <delete id="deleteUser" parameterType="int">
        delete from news.user where id = #{id};
    </delete>
</mapper>